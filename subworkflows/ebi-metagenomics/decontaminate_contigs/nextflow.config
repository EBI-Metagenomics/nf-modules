params {
    // Set default values for alignment filtering
    min_qcov = 0.5
    min_mapq = 20
}

process {
    withName: MINIMAP2_ALIGN {
        ext.args = "-x asm5 --paf-no-hit"
    }

    withName: SEQKIT_GREP {
        ext.args = "--invert-match"
    }
}